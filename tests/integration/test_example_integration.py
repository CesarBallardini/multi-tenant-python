"""Placeholder integration test -- replace once there's a real DB/service
to talk to (this test type is meant to run against real infrastructure,
not mocks)."""

import pytest


@pytest.mark.integration
def test_placeholder() -> None:
    assert True
